[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    local_data: requires large local test data under tests/data or Simulation/outputs
    snapshot: compares generated snapshot output against a saved baseline
